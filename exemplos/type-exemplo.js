"use strict";
function somarString(inputA, inputB) {
    if (typeof inputA === 'string' || typeof inputB === 'string') {
        return inputA.toString() + inputB.toString();
    }
}
function somarValor2(input1, input2) {
    let input = Number;
    let resultado = input1 + input2;
    return resultado;
}
